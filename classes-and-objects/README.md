Classes are designed to bring together related information and functionality

Class members:
  . Attributes
  . Methods

It's common to split function declation from definitions.
Methods are declared inside the class (in a header),
then define the methods outside the calss( in a .cpp).

main: Create ONJECTS using class names from hpp & cpp
hpp:  Declare Class (public and private)
cpp:  Define attributes and methods.

Object: An object is an instance of a class, which encapsultes data and functionality
pertaining to that data.

By default everything in class is PRIVATE.

Constructor: A constructor can instantiate an object with specific attributes.
Destructor: Object destruction is really about tidting up and preventing memorey leaks.

